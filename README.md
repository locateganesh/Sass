# SASS / SCSS

### Pseudo class:

The pseudo-class like `:hover`, `:focus`, `:active`, `:before`, `:after` should start with ampersand (&).

```
.footer h3{
    font-size: $font-md;
    &:hover{
      background: red; 
    }
}
```



